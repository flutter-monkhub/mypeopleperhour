# Vendored shared code

Copy of `../shared/src` (domain types, programme content, business rules) and `../shared/generated/seed.json` (demo data),
so this repo builds on its own (e.g. on Vercel). Do not edit here — change `../shared` and run `npm run sync-shared`.
